# Release Checklist

Before cutting a release:

1. `npm run typecheck`
2. `npm run build`
3. Update `CHANGELOG.md`
4. Confirm docs reflect current behavior
5. Confirm no secrets are committed
6. Tag version in `package.json`
7. Create GitHub release notes with migration notes

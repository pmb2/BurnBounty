# Private Materials Template

Copy this folder to `private/` in your local internal workspace.

`private/` is gitignored and intended for:

- production key management docs
- legal memos
- treasury and bankroll strategy
- internal contacts/escalation runbooks

Never store real secrets in version-controlled files.
